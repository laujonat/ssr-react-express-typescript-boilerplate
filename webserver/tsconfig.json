{
    "compilerOptions": {
        "lib": [ /* Declare compiler API declaration groups. */
            "es2017",
            "dom"
        ],
        "sourceMap": true, /* Generates corresponding '.map' file. */
        "allowSyntheticDefaultImports": true, /*  Allow default imports from modules with no default export. */
        "target": "esnext", /* Target latest version of ECMAScript. */
        "moduleResolution": "node", /* Search under node_modules for non-relative imports. */
        "forceConsistentCasingInFileNames": true,
        "strict": true, /* Enable strictest settings like strictNullChecks & noImplicitAny. */
        "allowJs": true, /* Process & infer types from .js files. */
        "noEmit": false, /* Don't emit; allow Babel to transform files. */
        "jsx": "react", /* Keep JSX syntax. */
        "outDir": "./build", /* Output directory */
        "esModuleInterop": true, /* Import non-ES modules as default imports. */
        "removeComments": true, /* Do not emit comments to output. */
        "baseUrl": "./" /* All module imports with non-relative names are assumed to be relative to the baseUrl.*/
    },
    "include": [ "client/**/*", "server/**/* "], /*  Defaults to excluding the node_modules, bower_components, jspm_packages and <outDir> directories when [exclude] not specified */
    "exclude": [ /* Include test directories here */
      "node_modules"
    ],
    "paths": {
        "*": [ /* Relative to baseUrl declaration. */
            "node_modules/*"
        ],
        "@client/*": [
            "client/*"
        ], /* import <module> from "@client/<target>" */
        "@server/*": [
            "server/*"
        ], /* import <module> from "@server/<target>" */
    },
    "typeRoots": [ /* Additional type declarations */
        "./node_modules/@types",
        "./client/types"
    ]
}
